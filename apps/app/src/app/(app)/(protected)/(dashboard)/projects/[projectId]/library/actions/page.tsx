import type { Metadata } from "next";

import { PageLayout } from "../../../../page-layout";

export const metadata: Metadata = {
  title: "Actions",
};

export default function ActionsPage() {
  return <PageLayout title="Actions" />;
}
