import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Links",
};

export default function LinksPage() {
  return <h1 className="text-2xl font-semibold tracking-tight">Links</h1>;
}
